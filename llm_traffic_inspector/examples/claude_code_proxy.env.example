# Use this in the shell where you run Claude Code.
# This is the placeholder credential that Claude Code sends to the local proxy.
# The proxy replaces it with the real upstream key from the proxy terminal.

export ANTHROPIC_BASE_URL="http://127.0.0.1:8788"
export ANTHROPIC_API_KEY="local-placeholder-only"

claude

